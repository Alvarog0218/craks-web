## Página en blanco

Reemplazar la página principal (`/`) con una página totalmente vacía de fondo blanco puro.

### Cambios
- **`src/pages/Index.tsx`**: Sustituir el placeholder actual por un componente que renderiza un `div` de pantalla completa (`min-h-screen`) con fondo blanco y sin contenido.

Eso es todo: la ruta `/` seguirá funcionando igual, pero mostrará un lienzo blanco vacío listo para añadir contenido más adelante.